name: SilentBullWatchApp
options:
  bundleIdPrefix: com.imsec
targets:
  SilentBullWatchApp:
    type: watchOS
    platform: watchOS
    sources: [Sources]
    resources: [Resources]
    info:
      path: Info.plist
      version: 1.0
      build: 1

